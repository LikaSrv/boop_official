<%# Adoption Section %>
<section class="adoption-section">
  <div class="adoption-container">
    <div class="adoption-header text-center">
      <h2 class="text-warning">ILS ATTENDENT UNE FAMILLE</h2>
      <p class="subtitle">Offrez un foyer à un animal qui en a besoin. Découvrez des compagnons à adopter et laissez Boop vous guider vers une adoption responsable et bienveillante.</p>
    </div>

    <div class="pets-scroll-container">
      <div class="pets-grid" data-controller="autoscroll" data-autoscroll-target="container" data-action="mouseenter->autoscroll#mouseEnter mouseleave->autoscroll#mouseLeave">
        <% @animals.each do |animal| %>
          <div class="pet-card">
            <div class="pet-image-container">
              <%= image_tag animal.photo, class: "pet-image" %>
            </div>
            <div class="pet-info">
              <h3><%= animal.name %></h3>
              <div class="pet-details">
                <span>Sex: <%= animal.sex %></span>
                <span>Age: <%= animal.age %> months</span>
              </div>
              <%= link_to "En savoir plus", animal_path(animal), class: "btn btn-primary mt-2" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>


  </div>
</section>
